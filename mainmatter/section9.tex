\section{Outlook}

How one could incorporate information geometry for similar problems

Any idea how to numerically resolve $F_{ij}$ for this flow?

Alternative choices than to model instead of $p(x|y)$ and $p(x)$?

The compressing network usually has a much heavier architecture. Once it has been trained, one can learn different lighter networks to map its summary to different target distributions. \red{This is only really true in the self-supervised case, see SKATR. So Maybe this should go in outlook}

\green{Hi Caroline, in case you're already reading this, I finally got the CNN to train on the 6-param inference problem as well! It even works if I attach a small MLP head to the end, which means I can hopefully train on varying bottleneck sizes the next days. I hope that the distinction in MI values will be clearer than for the gxH-inference case! $\smiley$} 
\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{graphics/Screenshot_5.png}
    \caption{MSE Pretraining Loss CNN}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{graphics/Screenshot.png}
    \caption{$\mathcal{L}_{\text{NPE}}$ Loss Pretraining Flow}
\end{figure}