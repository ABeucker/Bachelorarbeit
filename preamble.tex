% =========================================================
% FONT & LANGUAGE
% =========================================================
\usepackage{fontspec} % for XeLaTeX / LuaLaTeX

\usepackage[english, german]{babel}
\usepackage{microtype}

% =========================================================
% PAGE LAYOUT
% =========================================================
% \usepackage[
%   a4paper,
%   twoside,
%   inner=3cm,
%   outer=2.5cm,
%   top=2.5cm,
%   bottom=3cm,
%   headheight=14pt,
%   includeheadfoot
% ]{geometry}

\usepackage[left=25mm,right=25mm,top=25mm,bottom=25mm]{geometry}

% \usepackage[a4paper, total={15.5cm, 22cm}]{geometry}


% \usepackage{geometry}
%  \geometry{
%  a4paper,
%  total={170mm,257mm},
%  left=20mm,
%  top=20mm,
%  }

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em}

\setlength{\emergencystretch}{3em}

% =========================================================
% LINE SPACING
% =========================================================
\usepackage{setspace}
\onehalfspacing

% =========================================================
% COLORS
% =========================================================
\usepackage[dvipsnames]{xcolor}

% =========================================================
% CHAPTER / SECTION FORMATTING
% =========================================================
\usepackage{titlesec}

% % Chapter
% \titleformat{\chapter}
%   {\normalfont\bfseries\LARGE}
%   {\thechapter}
%   {1em}
%   {}

% \titlespacing*{\chapter}{0pt}{-10pt}{20pt}

% % Section
% \titleformat{\section}
%   {\normalfont\bfseries\Large}
%   {\thesection}
%   {1em}
%   {}

% \titlespacing*{\section}{0pt}{12pt}{6pt}

% % Subsection
% \titleformat{\subsection}
%   {\normalfont\bfseries\large}
%   {\thesubsection}
%   {1em}
%   {}

% \titlespacing*{\subsection}{0pt}{10pt}{4pt}

% % Subsubsection
% \titleformat{\subsubsection}
%   {\normalfont\bfseries\normalsize}
%   {\thesubsubsection}
%   {1em}
%   {}

% =========================================================
% HEADERS & FOOTERS
% =========================================================
\usepackage{fancyhdr}
\pagestyle{fancy}

\fancyhf{}

% chapter on even pages, section on odd pages
\fancyhead[LE, RO]{\leftmark}
% \fancyhead[RO]{\rightmark}
\fancyhead[RE,LO]{\thepage}

% \renewcommand{\headrulewidth}{0.4pt}

% keep marks clean
% \renewcommand{\chaptermark}[1]{\markboth{Chapter \thechapter\ #1}{}}
% \renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}

% define section marks only
\renewcommand{\sectionmark}[1]{%
    \markboth{\thesection\ #1}{}
}


% =========================================================
% FIGURES & TABLES
% =========================================================
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{tabularray}
\usepackage{adjustbox}
\usepackage{diagbox}

\setlength{\columnsep}{0.8cm}

% =========================================================
% MATH
% =========================================================
\usepackage{amsmath,latexsym, amssymb, dsfont}

% =========================================================
% LISTS
% =========================================================
\usepackage{enumitem}
\setlist[itemize]{noitemsep, topsep=2pt}
\setlist[enumerate]{noitemsep, topsep=2pt}

% =========================================================
% BIBLIOGRAPHY 
% =========================================================
\usepackage[backend=biber, sorting=none]{biblatex}
\addbibresource{backmatter/bibliography.bib}
\usepackage{csquotes}

% =========================================================
% CONTENT LISTS
% =========================================================
\usepackage{tocloft}
\setlength{\cftbeforesecskip}{2pt}

% =========================================================
% HYPERREF 
% =========================================================
\usepackage[
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue
]{hyperref}
% \usepackage[nameinlink,noabbrev]{cleveref}  % grouped references
% \crefrangeformat{figure}{[#3#1#4--#5#2#6]}
% \crefmultiformat{figure}
%   {[#2#1#3]}
%   {,[#2#1#3]}
%   {,[#2#1#3]}
%   {,[#2#1#3]}
